// convergence/render/webaudio — optional bundled instrument palette.
//
// Games may ignore this entirely and consume conductor events directly.
//
// Populated by later tasks: WebAudio instrument palette + ombak.
export {};
